@using PagedList.Mvc;

@model PagedList.IPagedList<KP.Models.Электрооборудование>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>

@using (Html.BeginForm("Index", "Электрооборудование", FormMethod.Get))
{
    <fieldset>
        Наименование: @Html.TextBox("family", "")
        <input type="submit" value="Найти" />
    </fieldset>
}

<table class="table">
    <tr>
        <th>
            Наименование
        </th>
        <th>
            Количество
        </th>
        <th>
            СуточноеВремяРаботы
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Наименование)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Количество)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.СуточноеВремяРаботы)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDЭлектрооборудование }) |
            @Html.ActionLink("Details", "Details", new { id=item.IDЭлектрооборудование }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IDЭлектрооборудование })
        </td>
    </tr>
}

</table>

Страница @(Model.PageCount < Model.PageNumber ? 0 : Model.PageNumber) из @Model.PageCount

@Html.PagedListPager(Model, page => Url.Action("Index", new { page }))