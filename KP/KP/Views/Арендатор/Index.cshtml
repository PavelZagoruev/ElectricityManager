@using PagedList.Mvc;

@model PagedList.IPagedList<KP.Models.Арендатор>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>

@using (Html.BeginForm("Index", "Арендатор", FormMethod.Get))
{
    <fieldset>
        Фамилия арендатора: @Html.TextBox("family", "")
        <input type="submit" value="Найти" />
    </fieldset>
}

<table class="table">
    <tr>
        <th>
            Фамилия
        </th>
        <th>
            Имя
        </th>
        <th>
            Отчество
        </th>
        <th>
            ПаспортныеДанные
        </th>
        <th>
            Договор.ИнформацияПоПомещению
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Фамилия)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Имя)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Отчество)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ПаспортныеДанные)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Договор.ИнформацияПоПомещению)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDАрендатор }) |
            @Html.ActionLink("Details", "Details", new { id=item.IDАрендатор }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IDАрендатор })
        </td>
    </tr>
}

</table>

Страница @(Model.PageCount < Model.PageNumber ? 0 : Model.PageNumber) из @Model.PageCount

@Html.PagedListPager(Model, page => Url.Action("Index", new { page }))