@using PagedList.Mvc;

@model PagedList.IPagedList<KP.Models.Арендодатель>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>

@using (Html.BeginForm("Index", "Арендодатель", FormMethod.Get))
{
    <fieldset>
        Наименование: @Html.TextBox("family", "")
        <input type="submit" value="Найти" />
    </fieldset>
}

<table class="table">
    <tr>
        <th>
            Наименование
        </th>
        <th>
            ПочтовыеДанные
        </th>
        <th>
            IDПомещения
        </th>
        <th>
            Договор.ИнформацияПоПомещению
        </th>
        <th>
            Здания.Наименование
        </th>
        <th>
            Сотрудники.Фамилия
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Наименование)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ПочтовыеДанные)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IDПомещения)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Договор.ИнформацияПоПомещению)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Здания.Наименование)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Сотрудники.Фамилия)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDАрендодатель }) |
            @Html.ActionLink("Details", "Details", new { id=item.IDАрендодатель }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IDАрендодатель })
        </td>
    </tr>
}

</table>

Страница @(Model.PageCount < Model.PageNumber ? 0 : Model.PageNumber) из @Model.PageCount

@Html.PagedListPager(Model, page => Url.Action("Index", new { page }))